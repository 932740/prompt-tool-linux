services:
  prompt-tool:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: prompt-tool
    ports:
      - "8088:8088"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
